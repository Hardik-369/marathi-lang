// While loop example
चल i = १
जोपर्यंत i <= ५ {
    मुद्रण("संख्या:", i)
    i = i + १
}

// For each loop example
चल यादी = [१, २, ३, ४, ५]
प्रत्येक item मध्ये यादी {
    मुद्रण("आयटम:", item)
}

// Function call in loop
कार्य वर्ग(x) {
    परत x * x
}

चल j = १
जोपर्यंत j <= ३ {
    मुद्रण("वर्ग:", वर्ग(j))
    j = j + १
}
